// CustomTypography.js
import { Typography } from '@mui/material';
import { styled } from '@mui/system';

export const ColorTextTypography = styled(Typography)(({ theme }) => ({
    color: '#0DFF00',  
    fontFamily: 'Courier',
}));